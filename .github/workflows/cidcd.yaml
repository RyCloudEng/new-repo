    name: conditonal job

    on: [push]

    jobs:
        jobs:
            runs-on: ubuntu-latest
            steps:
                - run: echo "hello from jobs"

        job2:
            runs-on: ubuntu-latest
            needs: jobs
            if: success()
            steps:
                - run: echo "job1 failed. hello from job 3"
        job3:
            runs-on: ubuntu-latest
            needs: job2
            if: failure()                     
            steps:
                - run: echo "job1 failed. hello from job 3"

